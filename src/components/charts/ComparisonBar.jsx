/**
 * ComparisonBar Component - Horizontal comparison of protein targets
 * Shows: RDA minimum, average, user target, athletic
 */
export const ComparisonBar = ({ userValue, rdaMin, average, athletic }) => {
  // Placeholder - will implement with Recharts
  // Shows where user falls on spectrum

  return (
    <div className="comparison-bar">
      {/* Bars will be rendered here */}
      <div className="chart-placeholder">Comparison Bar Chart</div>
    </div>
  );
};
