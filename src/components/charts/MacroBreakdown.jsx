/**
 * MacroBreakdown Component - Breakdown showing BMR → TDEE → Target
 * Visual waterfall showing the calculation steps
 */
export const MacroBreakdown = ({ bmr, tdee, target, deficit, surplus }) => {
  // Placeholder - will implement with Recharts
  // Shows waterfall: BMR → +Activity → TDEE → ±Goal adjustment → Target

  return (
    <div className="macro-breakdown">
      {/* Breakdown will be rendered here */}
      <div className="chart-placeholder">Macro Breakdown Chart</div>
    </div>
  );
};
